apiVersion: v1
kind: Service
metadata:
  name: postgres-service
  namespace: grade-submission
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/instance: postgres-db
  ports:
  - port: 5432
    targetPort: 5432
